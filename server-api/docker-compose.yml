version: '3.8'

services:
  roadmonitor_api:
    build: .
    ports:
    volumes:
      - persistent-database:/container/api/database
    restart: unless-stopped
      - "8001:8001"
    networks:
      - api-network

  nginx:
    image: nginx:alpine
    ports:
      - "8000:8000"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - roadmonitor_api
    networks:
      - api-network

networks:
  api-network:
    driver: bridge

volumes:
  persistent-database:
    external: true